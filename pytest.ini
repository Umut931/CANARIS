[pytest]
# Suite de tests CANARIS exécutable sans VM (générateur de canary, logique de
# détection, parsing VssGuard, faux positifs). Les tests VM-only sont dans
# HANDOFF.md, pas ici.
testpaths = tests
python_files = test_*.py
addopts = -ra
